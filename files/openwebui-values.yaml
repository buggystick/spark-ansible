service:
  type: ClusterIP
  port: 8080

persistence:
  enabled: true
  size: 10Gi
  storageClass: longhorn

env:
  - name: WEBUI_NAME
    value: "DGX Sparks WebUI (PP=2)"
  - name: OPENAI_API_BASE_URLS
    value: "http://vllm.default.svc.cluster.local:8000/v1"
